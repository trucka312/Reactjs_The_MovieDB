import PercentBox from './PercentBox'

export default {
  title: 'Percent',
  component: PercentBox,
}

const Template = (args) => <PercentBox {...args} />

export const Percent = Template.bind({})
Percent.args = {}
